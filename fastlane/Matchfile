git_url(ENV["MATCH_GIT_URL"])
storage_mode("git")
type("appstore")
app_identifier(ENV["APP_IDENTIFIER"] || "com.savagesbydesign.CathedralOS")
team_id(ENV["APPLE_TEAM_ID"])
# readonly mode is set per-lane in Fastfile (true for beta, false for bootstrap_signing)
